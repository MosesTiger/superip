version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/build-test
    overwrite: yes
permissions:
  - object: /var/www/build-test
    owner: ec2-user
    group: ec2-user
    mode: 755
hooks:
  AfterInstall:
    - location: deploy.sh
      timeout: 60
      runas: root